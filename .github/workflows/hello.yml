name: Action_Hello_World
on:
  push:
    branches: ["master"]

jobs: 
  build:
     
    runs-on: ubuntu-latest
    steps:
      -uses: action/checkout@v4
      -name: GitAction!
       run: pyton hello.py
