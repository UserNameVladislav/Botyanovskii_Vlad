name: Hello Git Actions v1!
on:
  push:
    branches:
    - master
 
jobs:
  prepare:
    runs-on: ubuntu-latest

    steps:
      - name: Output Actions
        uses: actions/upload-artifact@v3

      - run: python /github/Botyanovskii_Vlad/04.GitOps/hello.py 
